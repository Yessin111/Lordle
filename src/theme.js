import {createTheme} from "@mui/material";

const lorcanaTheme = createTheme({
    typography: {
        fontFamily: ['Brandon', 'Nunito', 'Roboto', '"Helvetica Neue"', 'Arial', 'sans-serif'].join(','),
    }
});

export default lorcanaTheme;
